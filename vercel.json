{
    "version": 2,
    "builds": [{
        "src": "package.json",
        "use": "@vercel/node",
        "config": {
            "includeFiles": ["dist/**", "src/generated/**", "prisma/**"]
        }
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "dist/main.js"
    }]
}